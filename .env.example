# ---------------------------------------------------------------------------
# All variables below are OPTIONAL and SERVER-SIDE ONLY.
# The site builds and renders completely without any of them, falling back to
# the local content in portfolio.ts. Never prefix any of these with
# NEXT_PUBLIC_ — that would ship the secret to the browser bundle.
# ---------------------------------------------------------------------------

# Notion CMS (Insights / Case Studies / AI Lab).
# Create an internal integration at https://www.notion.so/my-integrations,
# then share each database with it.
NOTION_TOKEN=
NOTION_ARTICLES_DATABASE_ID=
NOTION_CASE_STUDIES_DATABASE_ID=
NOTION_AI_LAB_DATABASE_ID=

# GitHub proof-of-work. Unauthenticated requests work (60/hr); a fine-grained
# token with public read-only access raises the limit for busy builds.
GITHUB_TOKEN=

# "Ask My Portfolio" generation providers. BOTH ARE OPTIONAL and both have a
# free tier. With neither set, the chatbot still retrieves and still cites —
# it just returns the relevant sources without prose around them.
# Groq is tried first (fastest); Gemini is the failover.
# https://console.groq.com/keys
GROQ_API_KEY=
# https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional model overrides. Providers retire hosted models on their own
# schedule, and a retired id fails as a 404 at request time rather than at
# build time — so these exist to make that a dashboard edit, not a patch.
# Defaults: llama-3.3-70b-versatile / gemini-3.6-flash.
GROQ_MODEL=
GEMINI_MODEL=

# Cost accounting for chatbot telemetry, in USD per million tokens. Both
# default to 0 because Groq and Gemini are used on their free tiers here — an
# invented rate in the logs would look like a measurement.
AI_USD_PER_MTOK_INPUT=
AI_USD_PER_MTOK_OUTPUT=
